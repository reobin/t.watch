{
  "name": "@reobin/t.watch",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "t.watch": "dist/index.js"
  },
  "files": [
    "dist",
    "integrations",
    "README.md"
  ],
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --outdir=dist && chmod +x dist/index.js",
    "prepack": "bun run build",
    "test": "bun test",
    "test:unit": "bun test"
  },
  "dependencies": {
    "@opentui/core": "^0.2.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
